1. Install PyQt5 using "pip install PyQt5" 
2. Change the createConnection function to add your sql databasename, username, hostname and password accordingly
3. Make sure the database is created before running the program [Import musicdb.sql OR Use Create_DB.txt to create schema and inset values].
4. Run runner.py to start the program.
